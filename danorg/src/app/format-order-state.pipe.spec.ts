import { FormatOrderStatePipe } from './format-order-state.pipe';

describe('FormatOrderStatePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatOrderStatePipe();
    expect(pipe).toBeTruthy();
  });
});
