import { Injector } from '@angular/core';
import { EditorView } from 'prosemirror-view';
import { Observable } from 'rxjs';
import { ContextMenuService } from './context-menu/context-menu.service';
import * as i0 from "@angular/core";
export interface CreateEditorViewOptions {
    onTextInput: (content: string) => void;
    element: HTMLElement;
    isReadOnly: () => boolean;
}
export declare class ProsemirrorService {
    private injector;
    private contextMenuService;
    editorView: EditorView;
    private mySchema;
    private enabled;
    /**
     * This is a Document used for processing incoming text. It ensures that malicious HTML is not executed by the
     * actual document that is attached to the browser DOM, which could cause XSS attacks.
     */
    private detachedDoc;
    constructor(injector: Injector, contextMenuService: ContextMenuService);
    contextMenuItems$: Observable<string>;
    createEditorView(options: CreateEditorViewOptions): void;
    update(text: string): void;
    destroy(): void;
    setEnabled(enabled: boolean): void;
    private getStateFromText;
    private getTextFromState;
    private configurePlugins;
    private getDetachedDoc;
    static ɵfac: i0.ɵɵFactoryDeclaration<ProsemirrorService, never>;
    static ɵprov: i0.ɵɵInjectableDeclaration<ProsemirrorService>;
}
