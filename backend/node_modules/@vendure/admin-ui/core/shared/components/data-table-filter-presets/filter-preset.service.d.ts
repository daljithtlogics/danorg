import { Observable } from 'rxjs';
import { DataTableConfig, LocalStorageService } from '../../../providers/local-storage/local-storage.service';
import * as i0 from "@angular/core";
export declare class FilterPresetService {
    private localStorageService;
    presetChanges$: Observable<Array<{
        name: string;
        value: string;
    }>>;
    private _presetChanges;
    constructor(localStorageService: LocalStorageService);
    protected getDataTableConfig(dataTableId: string): DataTableConfig;
    getFilterPresets(dataTableId: string): Array<{
        name: string;
        value: string;
    }>;
    saveFilterPreset(config: {
        dataTableId: string;
        name: string;
        value: string;
    }): void;
    deleteFilterPreset(config: {
        dataTableId: string;
        name: string;
    }): void;
    reorderPresets(dataTableId: string, fromIndex: number, toIndex: number): void;
    renameFilterPreset(config: {
        dataTableId: string;
        oldName: string;
        newName: string;
    }): void;
    static ɵfac: i0.ɵɵFactoryDeclaration<FilterPresetService, never>;
    static ɵprov: i0.ɵɵInjectableDeclaration<FilterPresetService>;
}
