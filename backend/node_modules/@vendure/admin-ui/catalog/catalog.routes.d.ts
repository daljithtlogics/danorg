import { Route } from '@angular/router';
import { PageService } from '@vendure/admin-ui/core';
export declare const createRoutes: (pageService: PageService) => Route[];
export declare function productOptionsEditorBreadcrumb(data: any, params: any): any;
